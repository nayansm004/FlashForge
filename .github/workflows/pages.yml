name: Deploy Web App to GitHub Pages

# Serves docs/index.html at https://<username>.github.io/<repo>/ automatically
# on every push to main. No build step needed — it's a static, self-contained file.
on:
  push:
    branches: [main]
    paths: ['docs/**']
  workflow_dispatch: {}

permissions:
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: docs
      - id: deployment
        uses: actions/deploy-pages@v4
